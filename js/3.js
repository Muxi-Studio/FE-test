
/**
 * @param {Array} arr
 * @return {boolean}
 */
function hasStraightFlush(arr) {
    for (let card of arr) {
        const kk = card % 13;
        const rank = Math.floor(card / 13);
        let isStraightflush = true




    }
}
